DATABASE_URL=""
DATABASE_AUTH_TOKEN=""

# If we want to run a local db
# DATABASE_URL="file:local.db"

# AES-128 key (base64), used to encrypt TOTP keys/recovery codes at rest.
# Generate with: openssl rand -base64 16
ENCRYPTION_KEY=""
